export default defineAppConfig({
    ui: {
       primary: 'teal',
    gray: 'cool',
    container: {
      constrained: 'max-w-5xl'
        },
      button: {
      default: {
        size: 'md',
      }
        },
      input: {
      default: {
              size: 'md',
      }
    }
  }
})