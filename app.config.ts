export default defineAppConfig({
    ui: {
       primary: 'lime',
    gray: 'zinc',
    container: {
      constrained: 'max-w-5xl'
        },
      button: {
      default: {
        size: 'md',
      }
        },
      input: {
      default: {
              size: 'md',
      }
    }
  }
})