export default defineAppConfig({
    ui: {
       colors: {
      primary: 'lime',
      neutral: 'zinc'
    },
    container: {
      constrained: 'max-w-5xl'
        },
      button: {
      default: {
        size: 'md',
      }
        },
      input: {
      default: {
              size: 'md',
      }
    }
  }
})